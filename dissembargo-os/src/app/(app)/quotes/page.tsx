import { PlaceholderPage } from "@/components/ui/PlaceholderPage";
import { FileText } from "lucide-react";

export default function QuotesPage() {
  return (
    <PlaceholderPage
      title="Quotes"
      description="Create, send, and track project quotes."
      icon={FileText}
    />
  );
}
