import { PlaceholderPage } from "@/components/ui/PlaceholderPage";
import { ClipboardList } from "lucide-react";

export default function ProjectsPage() {
  return (
    <PlaceholderPage
      title="Projects"
      description="Oversee active and completed creative projects."
      icon={ClipboardList}
    />
  );
}
