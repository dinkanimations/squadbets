import { PlaceholderPage } from "@/components/ui/PlaceholderPage";
import { Calendar } from "lucide-react";

export default function CalendarPage() {
  return (
    <PlaceholderPage
      title="Calendar"
      description="View team schedules, meetings, and milestones."
      icon={Calendar}
    />
  );
}
