
/**
 * Expo Router entry point.
 * 
 * Screens and navigation live in the app/ directory.
 * - Main/Home screen: app/index.tsx
 * - Other routes: app/<route>.tsx (e.g., app/leaderboard.tsx)
 * 
 * For web builds, index.html is generated at build time into the docs/ folder
 * (see package.json "build:web"). GitHub Pages is configured to deploy from docs/.
 */
import 'expo-router/entry';
