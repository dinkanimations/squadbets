{
  "name": "dissembargo-os-workspace",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix dissembargo-os",
    "build": "npm run build --prefix dissembargo-os",
    "start": "npm run start --prefix dissembargo-os",
    "lint": "npm run lint --prefix dissembargo-os",
    "db:types": "npm run db:types --prefix dissembargo-os"
  }
}
